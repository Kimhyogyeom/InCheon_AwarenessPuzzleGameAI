# 인공지능 인식 퍼즐 게임 — 기술설명서


## 프로젝트 개요

본 프로젝트는 인천학생과학관에 설치되는 체험형 전시 콘텐츠로, 관람객이 협동 로봇(Lebai LM3)과 퍼즐 대결을 펼치는 인터랙티브 게임 시스템이다. 관람객은 터치스크린을 통해 25피스 퍼즐을 완성하고, 동시에 로봇이 사전에 학습(Teaching)된 동작 시퀀스를 수행한다. 로봇이 동작을 완료하기 전에 퍼즐을 맞추면 사람이 승리하는 경쟁 구조를 통해, AI와 로봇 기술에 대한 흥미와 이해를 유도한다.


## 기술 스택

게임 엔진은 Unity 2022 이상(URP)을 사용하며, 프로그래밍 언어는 C#(.NET Standard 2.1)이다. 입력 처리는 Unity New Input System을 적용하여 마우스와 터치를 동시에 지원하고, UI는 uGUI와 TextMesh Pro를 활용한다. 로봇과의 통신은 HTTP POST 기반 JSON-RPC 2.0 프로토콜을 사용하며, C#의 HttpClient와 async/await 패턴으로 비동기 처리하여 UI 응답성을 보장한다.


## 게임 흐름

게임은 시작 화면(Ready) → 설명 화면(Description) → 게임 규칙(Puzzle) → 퍼즐 게임(PuzzleGame) 순서로 진행된다. 시작 화면에서 EASY(1배속) 또는 HARD(2배속) 모드를 선택하면, 선택한 난이도에 따라 로봇의 동작 속도가 조절된다. 게임이 시작되면 사람은 드래그 앤 드롭으로 퍼즐 조각을 배치하고, 로봇은 동시에 티칭 시퀀스를 수행한다. 타이머가 0이 되기 전에 25피스를 모두 맞추면 사람이 승리하고, 시간이 초과되면 로봇이 승리한다. 게임 종료 후에는 결과 화면에서 다시하기 또는 홈으로 돌아갈 수 있다.


## 핵심 모듈 구성

시스템은 역할별로 분리된 매니저 구조로 설계되어 있다. GameManager는 전체 게임 상태와 흐름을 관리하며, 무인 환경을 위한 30초 비활성 감지 타이머를 포함한다. PuzzleManager는 25피스 퍼즐의 랜덤 배치, 스냅 판정, 배치 카운트, 타이머 관리 및 로봇 대결 로직을 담당한다. UIManager는 패널 간 슬라이드 애니메이션 전환을 처리하고, SoundManager는 ScriptableObject 기반으로 BGM 2트랙과 SFX 6종을 관리한다.


## 로봇 연동

로봇 제어는 LebaiRobotController가 전담하며, Lebai LM3 로봇과 HTTP JSON-RPC 2.0으로 통신한다. 로봇의 동작은 JSON 파일 기반 티칭 시퀀스로 정의되어 있으며, 총 16세트의 정방향/역방향 티칭 파일이 StreamingAssets에 저장되어 있다. 이 구조 덕분에 Unity를 재빌드하지 않고 JSON 파일만 수정하여 로봇 동작을 현장에서 바로 조정할 수 있다. 지원하는 액션 타입은 관절 이동(move_joint), 기계식 그리퍼 제어(set_gripper), 버큠 그리퍼 디지털 출력(set_do), 대기(wait)이며, 각 스텝은 시작 시간과 소요 시간을 기반으로 순차 실행된다.


## 퍼즐 시스템

퍼즐은 5×5 그리드의 25개 조각으로 구성되며, 각 조각은 고유 인덱스로 대응하는 슬롯에만 배치할 수 있다. 게임 시작 시 조각들은 랜덤 위치에 겹치지 않게 배치된다. 사용자가 조각을 터치하면 크기가 2배로 확대되는 시각적 피드백이 제공되고, 슬롯 근처(80px 이내)에 드롭하면 스냅 판정이 이루어진다. 올바른 슬롯에 배치되면 조각이 고정되며, 배치 성공/실패에 따라 효과음이 재생된다.


## 키오스크 안정성

무인 전시 환경에서의 안정적 운영을 위해 여러 안전장치를 구현하였다. 게임 중 30초간 입력이 없으면 자동 패배 처리되고, 결과 화면에서 30초간 입력이 없으면 시작 화면으로 자동 복귀한다. 게임 종료 시에는 승패와 관계없이 로봇이 자동으로 정리 시퀀스를 실행하여 홈 포지션으로 복귀하며, 정리 중에는 안내 메시지를 표시한다. 모든 로봇 통신은 비동기로 처리되어 통신 지연이 발생해도 UI가 멈추지 않으며, 전체 JSON-RPC 요청과 응답은 로그 파일에 기록되어 디버깅에 활용할 수 있다.


## 하드웨어 구성

시스템은 Windows PC, 터치 디스플레이, Lebai LM3 6축 협동 로봇, 그리퍼(LMG-90 또는 버큠 그리퍼)로 구성되며, PC와 로봇은 유선 LAN(192.168.0.x 대역)으로 연결된다.
